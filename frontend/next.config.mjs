/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['gmpgjtjmalohyjsespkr.supabase.co'],
  },
  experimental: {
    optimizeCss: true,
  },
};

export default nextConfig;
